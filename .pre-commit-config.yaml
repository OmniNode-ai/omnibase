---
# === OmniNode:Metadata ===
# metadata_version: 0.1.0
# protocol_version: 1.1.0
# owner: OmniNode Team
# copyright: OmniNode Team
# schema_version: 1.1.0
# name: .pre-commit-config.yaml
# version: 1.0.0
# uuid: ab3651b0-3060-45e1-be81-fe9c99e9726a
# author: OmniNode Team
# created_at: '2025-05-22T17:18:16.672096'
# last_modified_at: '2025-05-22T21:19:13.356857'
# description: Stamped by MetadataYAMLHandler
# state_contract: state_contract://default
# lifecycle: active
# hash: 31430492c79a95a7271762547adf3f3ea37023e1c4c5b302660b423f9415b418
# entrypoint: python@.pre-commit-config.yaml
# runtime_language_hint: python>=3.11
# namespace: onex.stamped.pre_commit_config
# meta_type: tool
# === /OmniNode:Metadata ===

# OmniBase / ONEX .pre-commit-config.yaml
# See README.md for setup instructions

repos:
  # IMPORTANT: Metadata stamper MUST run FIRST before any formatters
  # to ensure idempotency. Formatters that run after stamping will
  # modify the stamped code and break idempotency.
  - repo: local
    hooks:
      - id: metadata-stamper
        name: ONEX Metadata Stamper
        entry: poetry run onex stamp file
        language: system
        files: \.(py|md|ya?ml|json)$
        exclude: >-
          ^\.pre-commit-config\.yaml$|
          src/omnibase/metadata/metadata_constants\.py|
          tests/schema/testdata/valid_onex_node\.yaml
        pass_filenames: true

  - repo: https://github.com/psf/black
    rev: 24.2.0
    hooks:
      - id: black
        language_version: python3.11

  - repo: https://github.com/charliermarsh/ruff-pre-commit
    rev: v0.4.4
    hooks:
      - id: ruff
        language_version: python3.11

  # Local mypy hook: uses poetry environment for type checking
  # Rationale: Ensures mypy runs in the same environment as dev/CI,
  # avoids config drift, and is standards-compliant for poetry-managed
  # projects.
  - repo: local
    hooks:
      - id: mypy-poetry
        name: mypy (poetry-managed type checking)
        entry: poetry run mypy
        language: system
        types: [python]
        pass_filenames: false
        args:
          - 'src/'

  - repo: https://github.com/pycqa/isort
    rev: 5.13.2
    hooks:
      - id: isort
        language_version: python3.11

  - repo: https://github.com/adrienverge/yamllint.git
    rev: v1.32.0
    hooks:
      - id: yamllint
        files: \.(yaml|yml)$

  - repo: local
    hooks:
      - id: pytest-pre-push
        name: Run pytest before push
        entry: scripts/pytest_pre_push.sh
        language: system
        types: [python]
        stages: [pre-push]
