config:
  scenario_config_version: "1.0.0"
  registry_tools: {}

chain:
  - node: node_manager
    input:
      maintenance_action: "invalid_action"
      target: "all"
    expect:
      status: "error"
      message: "Invalid maintenance action."
      errors:
        - "ONEX_CORE_001_INVALID_PARAMETER" 