config:
  scenario_config_version: "1.0.0"
  registry_tools: {}

chain:
  - node: node_manager
    input:
      contract_path: "invalid_contract.yaml"
      output_path: "models/state.py"
      options:
        force: true
    expect:
      status: "error"
      generated_files: []
      errors:
        - "SCHEMA_VALIDATION_FAILED" 