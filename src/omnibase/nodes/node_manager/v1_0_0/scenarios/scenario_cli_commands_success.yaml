config:
  version: "1.0.0"
  registry_tools: {}

chain:
  - node: node_manager
    input:
      cli_action: "generate"
      command_name: "run"
      args:
        - "--input"
        - "file.txt"
    expect:
      status: "success"
      cli_command: "run --input file.txt"
      errors: [] 