config:
  scenario_config_version: "1.0.0"
  registry_tools: {}

chain:
  - node: node_manager
    input:
      cli_action: "generate"
      command_name: ""
      args: []
    expect:
      status: "error"
      cli_command: null
      errors:
        - "INVALID_PARAMETER" 