---
scenario_name: "NodeManager Smoke Test"
description: "Minimal scenario to validate the reducer runs and returns success."
chain:
  - node: node_manager
    input:
      version: "1.0.0"
      input_field: "test"
    expect:
      version: "1.0.0"
      status: success
      message: "NodeManager ran successfully."
      output_field:
        processed: "test"
        backend: "test_backend"
config:
  version: "1.0.0"
  registry_tools: {} 