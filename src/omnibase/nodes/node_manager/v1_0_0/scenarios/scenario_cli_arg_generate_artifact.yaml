config:
  scenario_config_version: "1.0.0"
  registry_tools: {}

chain:
  - node: node_manager
    input:
      cli_action: "generate"
      command_name: "generate"
      args:
        - "--generate-artifact"
    expect:
      status: "success"
      cli_command: "generate --generate-artifact"
      errors: [] 