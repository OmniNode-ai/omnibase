---
scenario_name: "Template Node Smoke Test"
description: "Minimal scenario to validate the template_node reducer runs and returns success."
scenario_type: "smoke"
tags: ["template", "smoke", "reducer"]
version: "v1.0.0"
created_by: "auto"
node_versions:
  template_node: ">=1.0.0,<2.0.0"
---
chain:
  - node: template_node
    input:
      version: "1.0.0"
      template_required_field: "test"
      template_optional_field: "optional"
    expect:
      status: "success"
      message: "TemplateNode ran successfully." 