---
expected_validation: fail
scenario_name: "Template Node Invalid Version"
description: "Scenario to validate error handling for invalid version string."
scenario_type: "error"
tags: ["error", "version"]
version: v1.0.0
created_by: "auto"
node_versions:
  template_node: ">=1.0.0,<2.0.0"
chain:
  - node: template_node
    input:
      version:
        minor: 0
        patch: 0
      input_field: test
      optional_field:
        data:
          value: "optional"
    expect:
      version:
        minor: 0
        patch: 0
      status: error
      message: "Input should have required field 'major'"
      output_field:
        data: {} 