---
scenario_name: "Bootstrap Flow"
config:
  scenario_config_version: 1.0.0
  registry_tools:
    backend_selection: !!python/name:omnibase.nodes.node_kafka_event_bus.v1_0_0.tools.tool_backend_selection.ToolBackendSelection
    event_bus: !!python/name:omnibase.nodes.node_kafka_event_bus.v1_0_0.tools.tool_kafka_event_bus.KafkaEventBus
description: "Test node with --bootstrap argument. Should invoke bootstrap tool and publish canonical result."
scenario_type: "bootstrap"
tags: ["bootstrap", "eventbus"]
version: v1.0.0
created_by: "auto"
node_versions:
  node_kafka_event_bus: ">=1.0.0,<2.0.0"
chain:
  - node: node_kafka_event_bus
    input:
      version:
        major: 1
        minor: 0
        patch: 0
      input_field: test_bootstrap
      args: ["--bootstrap"]
    expect:
      status: success
      message: "*bootstrap*" 