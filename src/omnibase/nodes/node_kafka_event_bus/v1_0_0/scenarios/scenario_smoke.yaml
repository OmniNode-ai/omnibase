---
scenario_name: "Kafka Node Smoke Test"
description: "Minimal scenario to validate the reducer runs and returns success."
chain:
  - node: node_kafka_event_bus
    input:
      version: "1.0.0"
      input_field: test
      command_name: run
    expect:
      version: "1.0.0"
      status: success
      message: "NodeKafkaEventBus ran successfully."
      output_field:
        processed: "test"
        backend: "KafkaEventBus"
config:
  version: "1.0.0"
  registry_tools: {} 