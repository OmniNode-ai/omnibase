---
scenario_name: "Kafka Backend Selection Test"
description: "Scenario to validate that the node selects KafkaEventBus when provided with a Kafka config."
scenario_type: "integration"
tags: ["kafka", "backend", "eventbus"]
version: v1.0.0
created_by: "auto"
node_versions:
  node_kafka_event_bus: ">=1.0.0,<2.0.0"
chain:
  - node: node_kafka_event_bus
    input:
      version:
        major: 1
        minor: 0
        patch: 0
      config:
        bootstrap_servers: ["localhost:9092"]
        topics: ["onex-test-events"]
        client_id: "test-client"
        group_id: "test-group"
    expect:
      status: success
      message: "KafkaEventBus selected and operational."
      output_field:
        backend: "KafkaEventBus" 