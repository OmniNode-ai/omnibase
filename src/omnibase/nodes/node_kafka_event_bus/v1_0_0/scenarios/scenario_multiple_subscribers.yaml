---
scenario_name: "Multiple Subscribers"
config:
  version: "1.0.0"
  registry_tools:
    backend_selection: !!python/name:omnibase.nodes.node_kafka_event_bus.v1_0_0.tools.tool_backend_selection.ToolBackendSelection
    event_bus: !!python/name:omnibase.nodes.node_kafka_event_bus.v1_0_0.tools.tool_kafka_event_bus.KafkaEventBus
description: "Test node with multiple subscribers. All should receive the event."
scenario_type: "multi_subscriber"
tags: ["subscribers", "eventbus"]
version: v1.0.0
created_by: "auto"
node_versions:
  node_kafka_event_bus: ">=1.0.0,<2.0.0"
chain:
  - node: node_kafka_event_bus
    input:
      version:
        major: 1
        minor: 0
        patch: 0
      input_field: test_multi
      command_name: run
    expect:
      status: success
      message: "*"
      # Test harness should verify all subscribers receive event 