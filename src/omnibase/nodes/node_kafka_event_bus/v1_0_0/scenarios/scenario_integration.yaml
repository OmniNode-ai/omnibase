---
scenario_name: "Template Node Integration Context"
config:
  version: "1.0.0"
  scenario_config_version: 1.0.0
  registry_tools:
    backend_selection: !!python/name:omnibase.nodes.node_kafka_event_bus.v1_0_0.tools.tool_backend_selection.ToolBackendSelection
    event_bus: !!python/name:omnibase.nodes.node_kafka_event_bus.v1_0_0.tools.tool_kafka_event_bus.KafkaEventBus
description: "Scenario to validate integration context with external input."
scenario_type: "integration"
tags: ["integration", "external"]
created_by: "auto"
node_versions:
  node_kafka_event_bus: ">=1.0.0,<2.0.0"
chain:
  - node: node_kafka_event_bus
    input:
      version: "1.0.0"
      input_field: test_integration
      command_name: run
      integration: true
      config:
        bootstrap_servers:
          - "localhost:9092"
        client_id: "test-client"
        group_id: "test-group"
        topics:
          - "onex-test-events"
    expect:
      version: "1.0.0"
      status: success
      message: "NodeKafkaEventBus ran successfully."
      output_field:
        integration: true
        backend: "KafkaEventBus" 