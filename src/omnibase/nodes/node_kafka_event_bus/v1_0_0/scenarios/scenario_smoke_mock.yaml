---
scenario_name: "Template Node Smoke Test (Mock Registry)"
config:
  version: "1.0.0"
  registry_tools:
    backend_selection: !!python/name:omnibase.nodes.node_kafka_event_bus.v1_0_0.tools.tool_backend_selection.ToolBackendSelection
    event_bus: !!python/name:omnibase.runtimes.onex_runtime.v1_0_0.events.event_bus_in_memory.InMemoryEventBus
  trace_logging: true
description: "Minimal scenario to validate the reducer runs and returns success with mock registry."
scenario_type: "smoke"
tags: ["smoke", "reducer", "mock", "registry"]
version: v1.0.0
created_by: "auto"
node_versions:
  node_kafka_event_bus: ">=1.0.0,<2.0.0"
chain:
  - node: node_kafka_event_bus
    input:
      version: "1.0.0"
      input_field: test_mock
      command_name: run
    expect:
      version: "1.0.0"
      status: success
      message: "NodeKafkaEventBus ran successfully."
      output_field:
        processed: "test"
        backend: "KafkaEventBus" 